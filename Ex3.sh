# 获取用户输入
read -p "请输入一个正整数n: " n

# 判断是不是数字并且大于0
if [[ $n =~ ^[0-9]+$ && $n -gt 0 ]]; then
    for ((i = 1; i <= n; i++)); do

    #判断输入不是数字3，不能被3整除，不包含3
     if [[ $i != 3 && $((i % 3)) != 0 && $i != *3* ]]; then
            echo $i
        fi

    done
else
    echo "请输入有效的正整数。"
fi
